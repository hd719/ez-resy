{
  "name": "resy",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "start": "./env_manager.sh && npm run build && dotenv -e .env node dist/index.js",
    "start:today": "npm run build && dotenv -e .env node dist/index.js",
    "test": "npm run build"
  },
  "keywords": [],
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1",
    "dotenv": "^16.4.5",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "dotenv-cli": "^7.4.2",
    "typescript": "^5.9.2"
  }
}
